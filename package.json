{
  "name": "hounter",
  "version": "1.0.0",
  "main": "script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass scss:css --watch",
    "start": "npx live-server --watch=.",
    "dev": "npm-run-all --parallel sass start",
    "copy-assets": "node copy-assets.js",
    "compress": "node compress-assets.js",
    "dev-compress": "npm-run-all copy-assets compress serve",
    "serve": "http-server compressed -c-1 --brotli --gzip"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "compressing": "^2.0.0",
    "http-server": "^14.1.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.89.2"
  },
  "dependencies": {
    "fs-extra": "^11.3.1"
  }
}
